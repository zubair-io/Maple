// Barrel for Maple UI organisms lane A (collections/navigation +
// inspectors/panels, W6a+W6b). Filled by the lane's build wave; kept
// separate from public-api.ts so parallel lanes never write the same file.
export {};

// THIS FILE IS GENERATED BY tools/codegen.sh. DO NOT EDIT.
// Source: src/raw-pipeline/raw-core/src/ui_tokens.rs (COLOR_TOKENS, MOTION_TOKENS, RADIUS_TOKENS, SPACING_TOKENS).
//
// Pure value constants — hex strings, integer milliseconds, CSS easing
// strings. SwiftUI wrapping (`Color(hex:)`, `Animation.timingCurve(...)`)
// happens in `DesignTokens.swift`, which re-exports these under the
// stable `MapleTokens` / `MapleTokens.Motion` API surface.

import Foundation

public enum MapleUITokens {
    // MARK: - Color hex strings

    /// Window / app background.
    public static let bg: String = "#1c1917"
    /// Default panel surface.
    public static let surface: String = "#262524"
    /// Alternate / nested panel surface.
    public static let surfaceAlt: String = "#2e2c2a"
    /// Hover state for surface elements.
    public static let surfaceHover: String = "#3a3836"
    /// Sidebar / tree column background.
    public static let sidebar: String = "#292524"
    /// Text-input fill (matches window bg).
    public static let inputBg: String = "#1c1917"
    /// Image-canvas backdrop behind the loupe.
    public static let imageCanvas: String = "#141210"
    /// Primary text color.
    public static let textMain: String = "#e7e5e4"
    /// Secondary / metadata text color.
    public static let textMuted: String = "#a8a29e"
    /// Default border / divider.
    public static let border: String = "#44403c"
    /// Active ticks (drag-bar center tick), grab handles. Responsive-program S0a (#581).
    /// One tier up from `border` to preserve contrast headroom.
    public static let borderHi: String = "#5a5552"
    /// Brand primary (selection, primary action).
    public static let primary: String = "#c4493a"
    /// Dimmed primary (hover-of-selected, badge fill).
    public static let primaryDim: String = "#422016"
    /// Low-confidence signals (e.g. person detection chips). Tailwind amber-400.
    /// Responsive-program S0a (#581).
    public static let warn: String = "#fbbf24"
    /// Hover overlay (white at 6% opacity).
    public static let bgHover: String = "rgba(255, 255, 255, 0.06)"
    /// Active / pressed overlay (white at 10% opacity).
    public static let bgActive: String = "rgba(255, 255, 255, 0.1)"
    /// Success banner fill.
    public static let successBg: String = "rgba(34, 197, 94, 0.15)"
    /// Success banner text.
    public static let successText: String = "#4ade80"
    /// Error banner fill.
    public static let errorBg: String = "rgba(239, 68, 68, 0.15)"
    /// Error banner text.
    public static let errorText: String = "#f87171"
    /// Star-rating glyph color.
    public static let star: String = "#ef9f27"

    // MARK: - Motion

    /// Motion-token duration + easing pair. `ms` is integer milliseconds;
    /// `ease` is a CSS easing-function string. SwiftUI consumers translate
    /// these at the call site.
    public struct MotionSpec: Sendable, Hashable {
        public let ms: UInt32
        public let ease: String
        public init(ms: UInt32, ease: String) {
            self.ms = ms
            self.ease = ease
        }
        /// Seconds, as a `Double` — convenient for `Animation` constructors.
        public var seconds: Double { Double(ms) / 1000.0 }
    }

    public enum Motion {
        /// Drawer open/close (sidebar collapse on tablet/desktop). Not used on phone
        /// (tab-bar shell has no drawer).
        public static let drawer: MotionSpec = MotionSpec(ms: 240, ease: "cubic-bezier(0.22, 1, 0.36, 1)")
        /// Push transition (iOS-system feel).
        public static let push: MotionSpec = MotionSpec(ms: 320, ease: "cubic-bezier(0.32, 0.72, 0, 1)")
        /// Sheet present.
        public static let sheetPresent: MotionSpec = MotionSpec(ms: 320, ease: "cubic-bezier(0.32, 0.72, 0, 1)")
        /// Sheet dismiss.
        public static let sheetDismiss: MotionSpec = MotionSpec(ms: 280, ease: "cubic-bezier(0.32, 0.72, 0, 1)")
        /// Editor group/tool tab swap.
        public static let groupSwap: MotionSpec = MotionSpec(ms: 120, ease: "ease-in-out")
        /// Loupe chrome auto-hide.
        public static let chromeHide: MotionSpec = MotionSpec(ms: 180, ease: "ease-out")
        /// Library filter chip change cross-fade.
        public static let filterFade: MotionSpec = MotionSpec(ms: 120, ease: "linear")
    }

    // MARK: - Radius

    /// Chips, keyboard-hint pills, mention pills.
    public static let radiusXs: CGFloat = 4
    /// Icon buttons and hover targets.
    public static let radiusSm: CGFloat = 6
    /// Buttons, inputs, cards, toasts, widget containers — the workhorse default.
    public static let radiusMd: CGFloat = 8
    /// Composer, dialogs, floating panels.
    public static let radiusLg: CGFloat = 12
    /// Large feature cards.
    public static let radiusXl: CGFloat = 16
    /// Hero panels and large marketing-scale surfaces.
    public static let radiusXxl: CGFloat = 24
    /// Fully round — avatars, badges, filter chips.
    public static let radiusFull: CGFloat = 9999

    // MARK: - Spacing

    /// Icon padding.
    public static let spacingXs: CGFloat = 4
    /// Inline padding.
    public static let spacingSm: CGFloat = 8
    /// Buttons.
    public static let spacingMd: CGFloat = 16
    /// Card padding.
    public static let spacingLg: CGFloat = 24
    /// Section breaks.
    public static let spacingXl: CGFloat = 32
    /// Page-level spacing.
    public static let spacingXxl: CGFloat = 48
}

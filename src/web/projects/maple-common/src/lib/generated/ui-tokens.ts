// THIS FILE IS GENERATED BY tools/codegen.sh. DO NOT EDIT.
// Source: src/raw-pipeline/raw-core/src/ui_tokens.rs (COLOR_TOKENS, MOTION_TOKENS, RADIUS_TOKENS, SPACING_TOKENS).
//
// Pure value constants — hex strings, integer milliseconds, CSS easing
// strings. The `MapleTokens` and `MapleMotion` shapes used by app code
// live in `tokens.ts` and `motion.ts`, which re-export these under the
// stable public-api surface.

export const MAPLE_UI_COLORS = {
  /** Window / app background. */
  bg: '#1c1917',
  /** Default panel surface. */
  surface: '#262524',
  /** Alternate / nested panel surface. */
  surfaceAlt: '#2e2c2a',
  /** Hover state for surface elements. */
  surfaceHover: '#3a3836',
  /** Sidebar / tree column background. */
  sidebar: '#292524',
  /** Text-input fill (matches window bg). */
  inputBg: '#1c1917',
  /** Image-canvas backdrop behind the loupe. */
  imageCanvas: '#141210',
  /** Primary text color. */
  textMain: '#e7e5e4',
  /** Secondary / metadata text color. */
  textMuted: '#a8a29e',
  /** Default border / divider. */
  border: '#44403c',
  /**
   * Active ticks (drag-bar center tick), grab handles. Responsive-program S0a (#581). One tier
   * up from `border` to preserve contrast headroom.
   */
  borderHi: '#5a5552',
  /** Brand primary (selection, primary action). */
  primary: '#c4493a',
  /** Dimmed primary (hover-of-selected, badge fill). */
  primaryDim: '#422016',
  /**
   * Low-confidence signals (e.g. person detection chips). Tailwind amber-400.
   * Responsive-program S0a (#581).
   */
  warn: '#fbbf24',
  /** Hover overlay (white at 6% opacity). */
  bgHover: 'rgba(255, 255, 255, 0.06)',
  /** Active / pressed overlay (white at 10% opacity). */
  bgActive: 'rgba(255, 255, 255, 0.1)',
  /** Success banner fill. */
  successBg: 'rgba(34, 197, 94, 0.15)',
  /** Success banner text. */
  successText: '#4ade80',
  /** Error banner fill. */
  errorBg: 'rgba(239, 68, 68, 0.15)',
  /** Error banner text. */
  errorText: '#f87171',
  /** Star-rating glyph color. */
  star: '#ef9f27',
} as const;

export type MapleUIColorKey = keyof typeof MAPLE_UI_COLORS;

/** Motion-token duration + easing pair. */
export interface MapleMotionSpec {
  /** Duration in milliseconds. */
  readonly ms: number;
  /** CSS easing-function string. */
  readonly ease: string;
}

export const MAPLE_UI_MOTION = {
  /**
   * Drawer open/close (sidebar collapse on tablet/desktop). Not used on phone (tab-bar shell
   * has no drawer).
   */
  drawer: { ms: 240, ease: 'cubic-bezier(0.22, 1, 0.36, 1)' } as const,
  /** Push transition (iOS-system feel). */
  push: { ms: 320, ease: 'cubic-bezier(0.32, 0.72, 0, 1)' } as const,
  /** Sheet present. */
  sheetPresent: { ms: 320, ease: 'cubic-bezier(0.32, 0.72, 0, 1)' } as const,
  /** Sheet dismiss. */
  sheetDismiss: { ms: 280, ease: 'cubic-bezier(0.32, 0.72, 0, 1)' } as const,
  /** Editor group/tool tab swap. */
  groupSwap: { ms: 120, ease: 'ease-in-out' } as const,
  /** Loupe chrome auto-hide. */
  chromeHide: { ms: 180, ease: 'ease-out' } as const,
  /** Library filter chip change cross-fade. */
  filterFade: { ms: 120, ease: 'linear' } as const,
} as const;

export type MapleUIMotionKey = keyof typeof MAPLE_UI_MOTION;

export const MAPLE_UI_RADIUS = {
  /** Chips, keyboard-hint pills, mention pills. */
  xs: 4,
  /** Icon buttons and hover targets. */
  sm: 6,
  /** Buttons, inputs, cards, toasts, widget containers — the workhorse default. */
  md: 8,
  /** Composer, dialogs, floating panels. */
  lg: 12,
  /** Large feature cards. */
  xl: 16,
  /** Hero panels and large marketing-scale surfaces. */
  xxl: 24,
  /** Fully round — avatars, badges, filter chips. */
  full: 9999,
} as const;

export type MapleUIRadiusKey = keyof typeof MAPLE_UI_RADIUS;

export const MAPLE_UI_SPACING = {
  /** Icon padding. */
  xs: 4,
  /** Inline padding. */
  sm: 8,
  /** Buttons. */
  md: 16,
  /** Card padding. */
  lg: 24,
  /** Section breaks. */
  xl: 32,
  /** Page-level spacing. */
  xxl: 48,
} as const;

export type MapleUISpacingKey = keyof typeof MAPLE_UI_SPACING;

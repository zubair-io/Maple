# Vendored crate sources committed by `cargo vendor` (see ../.cargo/config.toml).
# Mark as vendored so GitHub keeps them out of language statistics, and suppress
# diffs so dependency-bump commits don't drown PRs in generated noise. These are
# upstream sources, not Maple code — they are not reviewed line-by-line here.
* linguist-vendored
* -diff
